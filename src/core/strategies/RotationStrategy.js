/**
 * Strategy for calculating rotation angles based on drag input
 */
export class RotationStrategy {
  calculateAngle(rotation, totalDeltaX, totalDeltaY) {
    throw new Error("Method 'calculateAngle()' must be implemented");
  }
}